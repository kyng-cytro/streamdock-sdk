{
  "name": "streamdock-sdk",
  "version": "0.0.1",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "bun test",
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist dist"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "node-hid": "^3.2.0",
    "sharp": "^0.34.3"
  }
}
